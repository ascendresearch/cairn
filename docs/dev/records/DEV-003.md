# DEV-003 DesignConformanceRecord

- Record status：`ReviewPending`
- 日期：2026-08-27
- Catalog：[`../SLICE_CATALOG.md`](../SLICE_CATALOG.md#2-st0--planning-readiness)
- Decision：[`D-041`](../../DECISIONS.md#d-041--historical-behavior-is-retained-through-newly-authored-sanitized-fixtures)

## 1. Identity

- Development Slice ID：`DEV-003`
- Title：首批 historical fixture sanitation、public/private disposition 与 ST1 graph fixture plan
- Stage / Integration Increment：`ST0 Planning Readiness`
- Owner role：`WS-QUALITY`
- Review roles：`WS-RECORD`、`WS-ADMISSION`；涉及原行为的 owning workstream 复核 obligation 语义
- Proposed status：保持 `Proposed`，本记录与 disposition 经审查后才申请 `Ready`
- Target branch/change set：本记录通过后的单一 DEV-003 change set；不夹带旧 production schema迁移或
  ST1 implementation

## 2. Objective 与非目标

- 单一 objective：对 D-041 指定的首批历史行为完成只读来源盘点，用一个被这些fixtures真实消费的最小
  `cairn-testkit` strong-type contract生成newly authored current-V1 public fixtures与provenance manifests，
  形成public/private/retain/delete disposition、acceptance scans和完整ST1 identity-graph fixture plan。
- 用户可观察或架构可验证的结果：每个可复用历史行为都有新作者、来源类别、MIT-compatible license/data
  classification、synthetic status、covered obligation、replacement scope和 future consumer；private或
  provenance-uncleared材料没有进入 Git。
- 明确非目标：不导入/改写 private state，不重用历史 digest，不建立 compatibility reader/converter，不
  删除尚无 accepted V1 consumer 的旧 fixture/code，不实现 ST1 graph，不声明 live replay/device behavior。
- 完成后仍 unknown/not-executed 的 scope：完整 ST1 graph bytes（DEV-105）、新架构 historical regression
  consumer（DEV-306）、live counterfactual、真实 provider/device lanes和 sealed Intent corpus（DEV-001）。

## 3. Traceability

- Requirement IDs：`FR-AGENT-005`、`FR-CAND-003`、`FR-EXEC-006`、`FR-EXEC-009`、`FR-REC-005`、
  `FR-REC-006`、`FR-REC-007`、`FR-REC-008`、`FR-REC-013`、`QR-AUD-001`、`QR-AUD-003`、
  `QR-SEC-004`、`QR-SEC-005`、`QR-MNT-007`、`QR-MNT-008`、`QR-OSS-001`。
- Decision IDs：`D-031`、`D-041`。
- System Design sections：[`RECORD_REPLAY.md` §8–10](../../RECORD_REPLAY.md#8-model-input-projection)、
  [`RECORD_REPLAY.md` §18–20](../../RECORD_REPLAY.md#18-security-and-privacy)、
  [`SYSTEM_REQUIREMENTS.md` §5.4–5.6](../../SYSTEM_REQUIREMENTS.md#54-candidate-search-and-gates)、
  [`SYSTEM_REQUIREMENTS.md` §5.10](../../SYSTEM_REQUIREMENTS.md#510-records-audit-replay-and-counterfactuals)、
  [`SYSTEM_REQUIREMENTS.md` §6.1、§6.4、§6.6](../../SYSTEM_REQUIREMENTS.md#61-auditability-and-correctness)。
- Focused design sections：[`CODE_ORGANIZATION.md` §9–10](../../design/CODE_ORGANIZATION.md#9-测试目录和责任)、
  [`CURRENT_BASELINE.md` §3–4](../CURRENT_BASELINE.md#3-可复用基础)。
- Open questions/blockers：D-041 已关闭 OQ-023；当前 blocker 是 disposition/review 尚未完成。OQ-024 不由
  本 slice 解决，sealed Intent controls归 DEV-001 restricted store。
- 被本 slice supersede/remove 的旧开发路径：changed fixture不沿用旧 digest、旧 admitted shape或旧 schema；
  只有对应新 V1 consumer accepted后才删除旧 product-specific code，删除 ownership见 §12.2。

## 4. Domain 与 Authority

| 项目 | Exact type/process/port | Authority | 不允许的能力 |
| --- | --- | --- | --- |
| Applicant/proposal | newly authored synthetic fixture body | historical behavior proposal/control | 自称 admitted fact；复制 private/raw provider bytes |
| Required obligations | D-041 七个 public families + provenance/disposition fields | D-041 / `QR-MNT-008` | 因当前代码缺失而省略 obligation |
| Planner/strategy | 不适用；固定 sanitation inventory | 无 | 模型重写private内容后称为 sanitized |
| Execution observation | 仅 recorded/synthetic expected observations | 原 accepted test/commit限定的 historical scope | 冒充 live provider、CUDA、Ascend或新 ST1 receipt |
| Mechanical Gate | 不实现 | future consuming slice | fixture中的 `passed`/expected outcome获得 authority |
| Admitted/output artifact | provenance manifest + disposition/scan receipt；不是 admitted product artifact | reviewer对 sanitation completeness的开发 authority | 变成 runtime admission输入而无 qualified consumer |
| Record/store | Git public fixtures；原 Git history/private store保持不可变 | Git/private store各自 authority | 读取/复制 `.cairn/secrets/**` bytes到 public tree |

## 5. Strong types 与 V1 影响

- 新增/修改 identities：新增相互不可替换的`FixtureIdentity`、`HistoricalSourceReference`和
  `SanitationScanProfileIdentity`；每个newly authored fixture和manifest由其当前bytes获得新identity，历史
  commit/path/digest只能作为provenance reference，不能成为新fixture identity。
- 容易混淆的 types/units/states：historical observation与current expectation、synthetic与recorded、public与
  restricted、source reference与source bytes、expected outcome与authoritative receipt必须分开。
- constructor/deserialization invariants：manifest/fixture strict V1 decoder拒绝 unknown fields、missing
  provenance/license/classification/obligation/replacement scope、absolute host paths、private classifications和
  noncanonical identity sets；deserialization重跑相同 validation。
- compile-fail/static boundary tests：`FixtureIdentity`不能传给要求`HistoricalSourceReference`或
  `SanitationScanProfileIdentity`的API；本slice不增加production port，DEV-101仍拥有public/restricted
  strong-port tests。Git/path/content scans和strict decode negative fixtures证明data boundary。
- V1 schemas/events/content domains/process protocols：新增唯一 current-V1 sanitation fixture manifest；不读取
  旧 schema、不修改 event/process protocol。
- 被删除的 V1 code/tests/fixtures：本 slice不删除；先生成 replacement plan，future consumer accepted后直接
  删除superseded path，不留 alias/dual reader。
- 确认无 version bump、alias、dual reader/writer、converter：是。

## 6. Inputs、Context 与 Data Policy

- Frozen public inputs：D-041、current requirement/design docs、Git commit `688b637`及当前 tracked source/test
  中的行为和测试断言。历史commit仅作read-only provenance。
- Restricted inputs：不读取case bytes；只盘点private类别与边界。sealed Intent cases归DEV-001，不成为本
  slice public fixture。
- Secret/external references：`.cairn/secrets/**`、原private stores和外部provider记录只记录categorical
  disposition，不记录文件内容、credential、request/response或绝对路径。
- Model/tool-visible projection：无需模型；scan工具只处理`git ls-files`返回的tracked public paths。不得把
  private tree作为scan输入后输出内容片段。
- Knowledge/skill snapshot：无。
- Previous feedback 与 contamination：新fixture从规范义务和当前Cairn-authored test behavior重新创作；
  complete third-party fetched bytes和uncleared Alloyport source不可作为authoring input。
- Hidden corpus/exposure policy：hidden corpus不属于public sanitation；只保留“存在于restricted store”的
  disposition，不发布identity、bytes或distinguishing diagnostics。
- 禁止进入该 role/process 的数据：credentials、PKI/enrollment bundle、SQLite/CAS/logs、raw provider
  requests/responses、private continuation、absolute host paths、uncleared third-party source和restricted case。

### 6.1 Initial read-only inventory observation

2026-08-27 的入口盘点只检查path/track状态，不读取private bytes：

- `git ls-files .cairn` 返回空；`.gitignore` 将 `.cairn/secrets/` 分类为ignored private tree；
- tracked filenames未发现credential、PEM/key、SQLite或`.cairn` material；
- 非文档tracked source/config中未命中 `/home/`、`/Users/`、`/data/projects/`、`/tmp/` host-path pattern；
- 这不是DEV-003 acceptance scan：尚未覆盖内容级secret canary、provider-body signature、生成fixture和Git
  history disposition。

## 7. Mechanisms 与 Qualification

| Mechanism | Exact identity | Qualification evidence | Scope/limitations | Requalification trigger |
| --- | --- | --- | --- | --- |
| strict fixture/manifest repository check | `cairn-testkit::fixtures::decode_manifest_v1` exact source/dependency identity | DEV-003 targeted missing/unknown-field/path/classification mutations；不是Admission qualification | 只验证sanitation contract | manifest或check source/dependency变化后重跑 |
| public/private/path/secret scan profile | `cairn-testkit::fixtures::scan_public_tree` + `sanitation-scan-profile.json` identity | seeded credential/path/provider-body canaries + clean control；不是Admission qualification | 不证明Git history/private store已删除 | pattern、scanner、encoding或scope变化后重跑 |
| provenance/disposition repository check | `cairn-testkit::fixtures::validate_provenance` exact source/dependency identity | missing license/source/author/obligation/replacement controls；不是Admission qualification | 不判断产品correctness | provenance schema或policy变化后重跑 |

以上机制在qualified前只能产生开发审查证据，不能成为未来Admission trust root。

## 8. Effects、Receipts 与 Recovery

- External effects：Git中新增public fixtures/manifests/scan summary；不修改private store和历史commit。
- Authorization point：本记录/disposition经review且catalog为`Ready`后才写fixture；任何删除另需对应new V1
  consumer accepted并由owning slice授权。
- Ambiguous-effect policy：中断的fixture draft不进frozen manifest；scan失败保持failed finding，不能过滤后
  重跑冒充原scope通过。
- Authoritative receipts：accepted commit、每个fixture current identity、provenance review、scan profile/
  scope/result和delete-deferred disposition。
- Commit/publish sequence：先锁定inventory/disposition，再newly author fixture，再strict validate/scan，最后
  更新manifest、record final review和baseline；不先删除旧路径。
- Crash/restart points：从immutable Git inputs和未变authoring spec重建；changed fixture必须新identity。
- Retry/idempotency identity：相同scan profile + tracked tree commit是同一scope；新commit/pattern产生新scan
  attempt，不覆盖旧finding。
- Cancellation/suspension：保留inventory，fixture保持draft且不供consumer使用。

## 9. Controls 与 Evidence Lanes

| Control/lane | Required? | Fixture/environment | Expected outcome | Evidence location |
| --- | --- | --- | --- | --- |
| Positive | 是 | 每个new public fixture + complete manifest | strict decode/provenance/disposition通过 | public fixture manifest |
| Negative | 是 | missing provenance/license/classification/obligation | fail closed | sanitation validator tests |
| Conflict | 是 | historical behavior与current contract不一致 | 保留两者并标记replacement scope，不merge | disposition manifest |
| Unknown | 是 | provenance或语义不足 | `PrivateOnly`/`DoNotImport`/`Deferred` | disposition manifest |
| Bypass/tamper | 是 | absolute path、secret/provider canary、old digest reuse | scan/identity check失败 | scan receipt |
| Fault/restart | 是 | incomplete draft/scan interruption | 不发布frozen manifest | workflow test/receipt |
| Recorded workflow | 是 | newly authored synthetic/recorded fixtures | 精确限定为recorded scope | fixture manifest |
| Live model | 否 | 不运行 | `NotExecuted` | final review |
| CUDA | 否 | 不运行 | `NotExecuted` | final review |
| Ascend build | 否 | 不运行 | `NotExecuted` | final review |
| Ascend NPU | 否 | 不运行 | `NotExecuted` | final review |
| Profiler/microbench | 否 | 不运行 | `NotExecuted` | final review |
| Model integration | 否 | DEV-603+ | `NotExecuted` | final review |

## 10. Budgets 与停止

- Turn/token/tool/wall/external budgets：无模型/网络/设备预算；source inventory以tracked files和Git history为界。
- CPU/CUDA/NPU budget：普通CPU decode/scan/test；CUDA/NPU为零。
- Diagnostic/hidden exposure budget：private材料零字节、零内容片段、零绝对路径输出；只允许类别级
  `PrivateOnly/DoNotImport` disposition。
- Stop/saturation policy：发现tracked secret/private body/absolute host path、license不清、old digest复用、
  fixture没有current consumer/obligation或需要兼容reader时停止并保持`Blocked`/`Proposed`。
- Budget exhaustion outcome：`Blocked`，不减少scan scope或将未执行family标为sanitized。

## 11. Backwards Audit 与 Impact

- Output → input/evidence edges：fixture → provenance manifest → historical behavior path+commit → requirement/
  decision obligation；scan receipt → exact tracked commit+scan profile。
- Common dependencies：current V1 codec/content identity、D-041 data policy、root MIT license。
- Feedback/knowledge/skill visibility edges：无skill；historical model/provider结果只可表示为synthetic/recorded
  behavior或external/private reference，不能复制body。
- Retraction/refutation impact：provenance/license refutation使对应fixture和consumer revalidation；scan profile
  refutation使该profile覆盖的所有fixture回到pending。
- Recorded replay scope：fixture只证明其冻结recorded inputs/expected transitions；不证明live provider相同。
- Live counterfactual changed variables：不执行；future fixture必须显式记录provider/model/profile/branch等
  changed variable和first divergence。

## 12. Acceptance

- Slice-specific exit criteria：§12.1 inventory每项有disposition；可立即重写的public families有new V1
  fixtures/provenance；private/uncleared材料只有categorical disposition；secret/path/provider scan通过；旧路径
  删除被明确defer到accepted consumer；ST1 graph plan完整但不伪造graph bytes。
- Applicable G0–G6 gates：G0写入前通过；G1适用strict V1和public/private path boundary；G2适用validator/
  scan/provenance机制的正负/tamper controls；G3适用draft/frozen与scan receipt restart；G4只需OfflineUnit和
  RecordedWorkflow；G5/G6适用。
- Required repository checks：strict decode、identity mutation、provenance/license、public/private disposition、
  secret/absolute-path/provider-body/uncleared-source scan、no compatibility reader、Markdown links、
  `git diff --check`和相关crate tests。
- Required external lanes：无。
- Documentation updates：本记录final review、catalog/current baseline状态、DEV-105 graph plan引用和DEV-306
  deferred deletion inventory。
- Fact-ledger update：accepted commit、fixture identities、scan profile/result、deferred items和remaining gaps。

### 12.1 Historical evidence inventory 与 planned public outputs

| D-041 family | Read-only source inventory | DEV-003 disposition | Planned public output |
| --- | --- | --- | --- |
| known false rejection / measured-family / blind spot | `crates/cairn-migration/src/historical.rs`、`reduction_control.rs`、`reduction_mutation.rs`、`reduction_admission.rs`、`reduction_candidate.rs`、`corpus_observation.rs`、`corpus_execution.rs`；`crates/cairn-migration/tests/historical_reduction_control.rs`；`src/bin/cairn-reduction-reference-fixture.rs`、`cairn-reduction-sequential-fixture.rs`、`cairn-reduction-tree-fixture.rs`、`cairn-reduction-zero-fixture.rs`、`cairn-reduction-drop-last-fixture.rs`、`cairn-reduction-offset-fixture.rs`和`src/bin/reduction_fixture_support/mod.rs` | `RewritePublic`; old production-specific shape retained only until DEV-306 consumer accepted | `fixtures/regressions/v1/oracle/historical-false-reject.json` |
| complete model-input audit + category missing | `crates/cairn-agent/src/lib.rs` input audit and tests；`crates/cairn-agent/src/step.rs`、`episode.rs` | `RewritePublic`; preserve domain-neutral runtime code | `fixtures/regressions/v1/record/model-input-audit.json` |
| recorded replay + same-input live divergence | `crates/cairn-agent/src/native_protocol.rs`、`step.rs`、`episode.rs`; current replay controls exist but complete live-counterfactual fixture is absent | `RewritePublicPartial`; recorded control now, live observation remains `NotExecuted` | `fixtures/regressions/v1/record/recorded-live-divergence.json` |
| recoverable wrong citation | `crates/cairn-migration/src/external_research.rs`、`oracle_tools.rs`、`oracle_workflow.rs`; exact recovery fixture absent | `RewritePublicSynthetic`; expected rejection/revision only, no raw provider body | `fixtures/regressions/v1/agent/wrong-citation-recovery.json` |
| output-capture failure | `crates/cairn-execution/src/coordinator.rs`、`contract.rs`；`crates/cairn-migration/src/call_adapter.rs`及`tests/host_call_adapter.rs` | `RewritePublic`; preserve generic execution code | `fixtures/regressions/v1/execution/output-capture-failure.json` |
| stale lease | `crates/cairn-execution/src/assignment.rs`、`control.rs`、`coordinator.rs` | `RewritePublic`; preserve generic execution code | `fixtures/regressions/v1/execution/stale-lease.json` |
| duplicate live worker | `crates/cairn-execution/src/worker.rs`、`control.rs` | `RewritePublic`; preserve generic worker code | `fixtures/regressions/v1/execution/duplicate-live-worker.json` |
| complete ST1 identity graph | 当前不存在；contract来源为DEV-100..105与DEV-004 record | `PlanOnly`; DEV-105 acceptance时author/freeze，DEV-003不得造假 | `fixtures/regressions/v1/workflows/st1-identity-graph.json`（reserved, not created in DEV-003） |

共同新增：

- `fixtures/regressions/v1/README.md`；
- `fixtures/regressions/v1/manifest.json`；
- `fixtures/regressions/v1/sanitation-scan-profile.json`；
- `fixtures/regressions/v1/st1-identity-graph-plan.md`。

Validator/check code additions：

- root `Cargo.toml` workspace member；
- `crates/cairn-testkit/Cargo.toml`；
- `crates/cairn-testkit/src/lib.rs`；
- `crates/cairn-testkit/src/fixtures/mod.rs`；
- `crates/cairn-testkit/src/fixtures/provenance.rs`；
- `crates/cairn-testkit/src/fixtures/sanitation.rs`；
- `crates/cairn-testkit/tests/fixture_sanitation.rs`；
- `Cargo.lock` only if dependency resolution changes its current bytes。

该crate不是空framework：上述public fixtures是当前consumer，slice acceptance直接执行其strict decode、
provenance、identity mutation和seeded scan controls。它不得导出production-only bypass constructor或持有
restricted-store capability。

每个JSON fixture必须在manifest中独立列出current author、historical behavior path+commit、license/data
classification、synthetic/recorded status、covered obligation、replacement scope、consumer slice和current bytes
identity。reserved ST1 path只写入plan，不作为manifest中的existing artifact。

### 12.2 Retain/delete/private disposition

| Material | Current disposition | Delete owner/condition |
| --- | --- | --- |
| `crates/cairn-migration/src/historical.rs`、`reduction_control.rs`、`reduction_mutation.rs`、`reduction_admission.rs`、`reduction_candidate.rs`、`corpus_observation.rs`、`corpus_execution.rs` | `RetainUntilConsumerAccepted` | DEV-306 accepted后删除superseded product-specific paths；generic mechanics经review迁移，不保留alias |
| `crates/cairn-migration/src/bin/cairn-reduction-reference-fixture.rs`、`cairn-reduction-sequential-fixture.rs`、`cairn-reduction-tree-fixture.rs`、`cairn-reduction-zero-fixture.rs`、`cairn-reduction-drop-last-fixture.rs`、`cairn-reduction-offset-fixture.rs`、`reduction_fixture_support/mod.rs` | `RetainUntilConsumerAccepted` | DEV-306/new regression runner accepted后删除被替代binaries |
| `crates/cairn-migration/tests/historical_reduction_control.rs` | `RetainUntilConsumerAccepted` | DEV-306 accepted且new regression覆盖原obligation后删除 |
| domain-neutral agent/record/execution controls | `RetainReusableFoundation` | 不由DEV-003删除；future owning slice只更新current V1 contract |
| fixed `matmul-zero-k` materialization | `RetainHistoricalTransportControl` | 只有replacement transport consumer accepted后另行删除；不作为Intent operator |
| `.cairn/secrets/**` credentials/PKI/enrollment/SQLite/CAS/logs/provider/private continuation | `PrivateOnlyNeverImport` | 不由Git change删除或复制；原private-store lifecycle另行管理 |
| complete third-party fetched bytes / uncleared Alloyport source | `DoNotImport` | 无public delete；若存在于private origin保持private或按原store policy处理 |
| restricted sealed Intent cases | `OwnedByDEV001RestrictedStore` | 不进入本slice/public fixture tree |

## 13. G0 Review Checklist

- [x] D-041 blocker已关闭；scope不依赖OQ-024答案。
- [x] objective、non-goals、inputs、outputs、authority、effects和exact inventory已列出。
- [x] V1/no-compatibility、private boundary、external lanes、budget和deferred delete conditions已声明。
- [x] initial path-level inventory完成且明确不是acceptance scan。
- [ ] WS-QUALITY owner确认七个family的rewrite/plan scope。
- [ ] WS-RECORD reviewer确认record/replay、identity/provenance与scan receipt边界。
- [ ] WS-ADMISSION reviewer确认restricted material零导入和fixture无admission authority。
- [ ] relevant owning workstreams确认old paths的retain/delete condition。
- [ ] reviewer确认开始实施前worktree audit，并把catalog/current baseline改为`Ready`。

## 14. Final Review（完成时填写）

- Accepted commit(s)：未填写
- Evidence identities/paths：未填写
- 与初始设计的偏差：未填写
- Remaining gaps：未填写
- Revalidation triggers：D-041 policy、fixture bytes/manifest schema、historical provenance/license、scanner
  profile/scope或consumer obligation任一变化
- Final status：`NotStarted`
