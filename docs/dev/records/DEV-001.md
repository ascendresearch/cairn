# DEV-001 DesignConformanceRecord

- Record status：`ActiveConformance`
- 日期：2026-08-27
- Catalog：[`../SLICE_CATALOG.md`](../SLICE_CATALOG.md#2-st0--planning-readiness)
- Decision：[`D-039`](../../DECISIONS.md#d-039--the-first-intent-admission-operator-is-a-clean-room-finite-f32-reduction)

## 1. Identity

- Development Slice ID：`DEV-001`
- Title：物化首个 clean-room finite-f32 reduction source、claim controls 与 sealed corpus
- Stage / Integration Increment：`ST0 Planning Readiness`
- Owner role：`WS-DOMAIN`
- Review roles：`WS-QUALITY`、`WS-ADMISSION`；source/license 部分由一个非作者 reviewer 复核
- Proposed status：`InProgress`；review package `fe88f4e`已接受，DEV-003依赖由`79a1174`满足，
  2026-08-27从clean worktree启动
- Target branch/change set：本记录通过后的单一 DEV-001 change set；不得与 DEV-002 qualification 或
  DEV-100 crate replacement 混合

## 2. Objective 与非目标

- 单一 objective：从 D-039 规范文字独立创作并冻结第一个 operator 的 exact CUDA/host source、公开
  claim/corpus controls、六分区 restricted sealed batch 和 provenance/disposition metadata。
- 用户可观察或架构可验证的结果：reviewer 能仅从新创作 bytes 和 manifests 重建 first-domain、ABI、
  competing hypotheses、公开 controls、sealed partition completeness 与 content identities，且无需读取
  Alloyport source 或私有部署状态。
- 明确非目标：不实现 SIR、Admission、`MigrationIntentContract`、Oracle portfolio、Ascend C candidate、
  mechanism qualification、adaptive hidden queries、真实 CUDA/Ascend execution 或 performance policy。
- 完成后仍 unknown/not-executed 的 scope：CUDA build/run、Ascend build/NPU、真实 device numerical
  behavior、一般 adaptive replenishment（OQ-024）、D-040 mechanism qualification，以及 D-039 明确排除的
  empty/subnormal/non-finite/alias/wider-shape domain。

## 3. Traceability

- Requirement IDs：`FR-TASK-005`、`FR-TASK-006`、`FR-TASK-007`、`FR-INTENT-002`、`FR-INTENT-003`、
  `FR-INTENT-005`、`FR-INTENT-006`、`FR-INTENT-010`、`FR-INTENT-011`、`FR-INTENT-012`、
  `FR-INTENT-013`、`FR-REC-013`、`QR-AUD-003`、`QR-SEC-004`、`QR-SEC-005`、`QR-MNT-007`、
  `QR-MNT-008`、`QR-OSS-001`。
- Decision IDs：`D-031`、`D-039`、`D-040`、`D-041`。
- System Design sections：[`SYSTEM_DESIGN.md` P1/P2/P8/P10/P11](../../SYSTEM_DESIGN.md#2-核心设计原则)、
  [`SYSTEM_DESIGN.md` §7.2](../../SYSTEM_DESIGN.md#72-高阶意图恢复与准入)、
  [`SYSTEM_DESIGN.md` §8](../../SYSTEM_DESIGN.md#8-semantic-intent-recovery)、
  [`SYSTEM_DESIGN.md` §14](../../SYSTEM_DESIGN.md#14-record证据与-replay)、
  [`SYSTEM_REQUIREMENTS.md` §5.1–5.2](../../SYSTEM_REQUIREMENTS.md#51-task-intake-and-identity)。
- Focused design sections：[`SEMANTIC_INTENT_RECOVERY_DESIGN.md` §3–7、§12](../../oracle/SEMANTIC_INTENT_RECOVERY_DESIGN.md#3-用户意图的分层模型)、
  [`ADMISSION_ARCHITECTURE.md` §20](../../design/ADMISSION_ARCHITECTURE.md#20-首期范围与实施边界)、
  [`CODE_ORGANIZATION.md` §9–10](../../design/CODE_ORGANIZATION.md#9-测试目录和责任)。
- Open questions/blockers：D-039 已关闭 OQ-016；只有采用 adaptive query 才受 OQ-024 阻塞，本 slice
  固定为 non-adaptive sealed batch。DEV-003 current-V1 fixture provenance/sanitation contract已由
  `79a1174ad9767ab528c808a39511ada91e8129f9`接受；不存在待选规范答案或未满足的entry blocker。
- 被本 slice supersede/remove 的旧开发路径：不导入 Alloyport `cuda-reduction-v1` source bytes；不把
  `matmul-zero-k` 或当前 `cairn-migration` historical reduction schema 当作 first Intent schema。DEV-001
  不删除旧路径，删除时机归 DEV-100/DEV-306。

## 4. Domain 与 Authority

| 项目 | Exact type/process/port | Authority | 不允许的能力 |
| --- | --- | --- | --- |
| Applicant/proposal | `claims.json` 中的 current-V1 fixture records | 新创作的 proposal/control material，无 admission authority | 创建 admitted outcome、读取 restricted cases |
| Required obligations | D-039 first domain、ABI、hypothesis 与 partition checklist | D-039 / `FR-INTENT-013` | 作者删减六分区或把 excluded domain 当 accepted |
| Planner/strategy | 不适用；固定 deterministic fixture recipe | 无 planner authority | adaptive query、按结果追加 hidden case |
| Execution observation | 本 slice 不产生 device observation | 无 | 把 source review、host arithmetic 或 recorded bytes 声称为 CUDA/NPU receipt |
| Mechanical Gate | 不实现；DEV-104 的后续消费者 | Admission authority（未来） | DEV-001 fixture 自行 gate/admit |
| Admitted/output artifact | exact public bundle identity + restricted manifest identity；不是 admitted intent | Git commit authority与 restricted-store review authority各自限定 | public commit 泄露 hidden bytes；identity 冒充 verdict |
| Record/store | Git public fixture tree；`.cairn/secrets/restricted-corpora/**` private tree | public reviewer / restricted-store custodian | public code读取 private path；把 digest knowledge当 capability |

## 5. Strong types 与 V1 影响

- 新增/修改 identities：不新增 production identity type；public bundle 和每个 restricted artifact 的
  content identity 只能在 bytes 最终冻结后计算，禁止预填历史 digest。
- 容易混淆的 types/units/states：input length 与 byte length、`f32` bits 与 mathematical real value、
  public/restricted case identity、hypothesis 与 admitted claim、excluded 与 unknown/user-decision 必须分开。
- constructor/deserialization invariants：本 slice 的 raw JSON 仅是 fixture/codec boundary；provenance和
  sanitation字段必须消费DEV-003 accepted `cairn-testkit` contract；未来product validator必须strict V1、
  拒绝 unknown fields，并验证 finite normal-or-signed-zero、范围、`1 <= N <= 256`、
  canonical ordering、case uniqueness、partition completeness 和 public/restricted classification。不得让
  未验证 map/JSON 进入后续 domain logic。
- compile-fail/static boundary tests：DEV-001 不新增 production API；public/restricted typed port 的
  compile-fail proof 由 DEV-101 拥有。本 slice 必须以 repository/path audit 证明 restricted bytes 不在 Git。
- V1 schemas/events/content domains/process protocols：只新增 current-V1 fixture manifests；不修改 production
  event、CAS domain 或 process protocol。
- 被删除的 V1 code/tests/fixtures：本 slice无删除；旧 material 的后续 disposition 见 DEV-003。
- 确认无 version bump、alias、dual reader/writer、converter：是；直接定义唯一 current V1 fixture shape。

## 6. Inputs、Context 与 Data Policy

- Frozen public inputs：D-039、D-040、D-041、`FR-INTENT-013`、root MIT license、当前 canonical codec/content
  identity rules；不得以当前实现行为覆盖这些规范。
- Restricted inputs：仅由独立作者从 D-039 六个 partition descriptions 新创作的 sealed cases；其 bytes 和
  distinguishing metadata 不进入 Git、public CAS、模型输入、日志或本记录。
- Secret/external references：无外部 source reference；private root 固定为
  `.cairn/secrets/restricted-corpora/intent/reduce-sum-f32/v1/`，但代码和 public manifest 不硬编码可用
  credential/capability。
- Model/tool-visible projection：本 slice 不调用模型；工具只可见规范和新创作 public bytes。restricted
  reviewer 工具运行在 private boundary，public 输出仅为 partition completeness/disposition receipt。
- Knowledge/skill snapshot：无；规范文件和 commit identity 是唯一创作输入记录。
- Previous feedback 与 contamination：可读取当前 Cairn 对 historical false reject/blind spot 的抽象描述来
  选择控制类别；不得复制 provenance 未清的 Alloyport source、完整第三方 fetched bytes 或 private provider
  body。
- Hidden corpus/exposure policy：non-adaptive sealed batch；每个 required partition 至少一个独立审查 case；
  只发布最小 claim/outcome diagnostic；distinguishing disclosure 触发 burn-to-public-regression，并在缺分区时
  阻止下一次 attempt，不能自适应补问。
- 禁止进入该 role/process 的数据：`.cairn/secrets/**` 既有部署材料、credentials/PKI、SQLite/CAS、logs、
  raw provider requests/responses、private continuation、uncleared source bytes 和绝对 host paths。

## 7. Mechanisms 与 Qualification

DEV-001 冻结 DEV-002 qualification contract 的 exact artifact/corpus inputs，不为任何 verifier 建立
qualification authority，也不预填尚不存在实现的 identity/receipt。

| Mechanism | Exact identity | Qualification evidence | Scope/limitations | Requalification trigger |
| --- | --- | --- | --- | --- |
| Fixture strict decoder/validator | DEV-002 冻结 semantic slot；DEV-100 绑定 implementation identity | DEV-002 controls + DEV-100 receipt | 只验证 fixture shape/invariants，不决定 intent | schema、source、invariant、dependency 变化 |
| CUDA/host ABI static-fact extractor | DEV-002 冻结 semantic slot；DEV-102 绑定 implementation identity | DEV-002 controls + DEV-102 receipt | 不证明运行行为 | source grammar、extractor source/toolchain/limitation 变化 |
| Corpus identity/partition checker | DEV-002 冻结 control contract；owning consumer 绑定 implementation identity | DEV-002 controls + owning receipt | public 与 restricted 分开运行 | case bytes、partition policy、canonicalization 变化 |

## 8. Effects、Receipts 与 Recovery

- External effects：Git 中新增 public fixture bytes；private store 中新增 restricted bytes。无网络、模型、
  device 或生产服务 effect。
- Authorization point：本记录经具名 review roles 审查且 catalog 改为 `Ready` 后，才允许创作/写入上述
  source 和 fixture；restricted写入另需 private-store custodian 授权。
- Ambiguous-effect policy：Git/private write 中断只产生未提交 draft；不得把不完整 bundle 标记 frozen。
- Authoritative receipts：accepted commit、public content-identity report、private partition completeness/
  provenance review receipt；private receipt 不含 case bytes或 distinguishing diagnostics。
- Commit/publish sequence：先冻结 public draft并审查 provenance，再冻结 restricted batch并私下审查，
  最后生成各自当前 bytes 的 identity 和 public disposition summary；不从 public tree复制 hidden case。
- Crash/restart points：任何中断从最后一个未变 frozen input重新生成；changed bytes 必须获得新 identity。
- Retry/idempotency identity：相同 canonical bytes 得到相同 content identity；changed bytes 是新 artifact，
  不覆盖已引用 identity。
- Cancellation/suspension：保留 draft但不进入 manifest `frozen` set、不供 DEV-002 使用。

## 9. Controls 与 Evidence Lanes

| Control/lane | Required? | Fixture/environment | Expected outcome | Evidence location |
| --- | --- | --- | --- | --- |
| Positive | 是 | public honest/tail cases | domain、ABI、real-sum hypothesis完整 | public corpus + review receipt |
| Negative | 是 | wrong exact-bit、wrong deployment-specialization | 保留为 competing/refuted hypothesis，不被静默采纳 | public claims/corpus |
| Conflict | 是 | caller/source conflict | `NeedsUserDecision` fixture disposition | public user-decision controls |
| Unknown | 是 | excluded/insufficient-authority cases | 显式 unknown/excluded，不产生 admitted claim | public claims/corpus |
| Bypass/tamper | 是 | restricted tamper/wrong-binding partition | binding failure且不泄露 case | private receipt |
| Fault/restart | 否 | 无 durable runtime | `NotApplicable(no-runtime-lifecycle)` | final review |
| Recorded workflow | 否 | DEV-104 | `NotExecuted` | final review |
| Live model | 否 | 不使用 | `NotApplicable(no-model)` | final review |
| CUDA | 否 | DEV-104 runner calibration / DEV-402 | `NotExecuted`，不得推断 build/run | final review |
| Ascend build | 否 | DEV-403 | `NotExecuted` | final review |
| Ascend NPU | 否 | DEV-404 | `NotExecuted` | final review |
| Profiler/microbench | 否 | ST5 | `NotExecuted` | final review |
| Model integration | 否 | ST6 | `NotExecuted` | final review |

## 10. Budgets 与停止

- Turn/token/tool/wall/external budgets：无模型或外部服务预算；评审需至少 owner + 非作者 quality reviewer。
- CPU/CUDA/NPU budget：只允许普通 CPU canonicalization/validation；CUDA/NPU 均为零。
- Diagnostic/hidden exposure budget：public diagnostic 只允许 partition 名、完成/失败和 disposition；零 case
  bytes、零 distinguishing input/output。
- Stop/saturation policy：任一公开控制类缺失、六分区任一缺失、provenance 未清、source 与 D-039 冲突、
  private bytes 出现在 Git diff 或 reviewer 不独立即停止。
- Budget exhaustion outcome：`Blocked`，不得缩减 required partition 或把 lane 记为通过。

## 11. Backwards Audit 与 Impact

- Output → input/evidence edges：public file → public manifest → D-039/author/license；restricted file → private
  manifest → D-039 partition/reviewer；public disposition summary只引用 private review receipt identity。
- Common dependencies：D-039 domain/ABI/numerical policy、current V1 canonicalization 和 root MIT license。
- Feedback/knowledge/skill visibility edges：无 knowledge/skill；historical behavior仅作为具名 provenance note，
  不携带旧 bytes或 authority。
- Retraction/refutation impact：source provenance/license refutation使整个 public source bundle失效；某 restricted
  case refutation只影响其 case和 partition completeness，缺分区会阻止后续 attempt。
- Recorded replay scope：无 workflow replay；canonical bundle identity可离线重算。
- Live counterfactual changed variables：不适用；任何 live run 是 DEV-104/DEV-402 的新 attempt。

## 12. Acceptance

- Slice-specific exit criteria：D-039 source/host tree、public claim/corpus/user-decision controls、六分区
  restricted manifest和所有 provenance/disposition records均冻结；content identities由最终 bytes派生；
  public Git中没有 restricted/private/uncleared material。
- Applicable G0–G6 gates：G0 必须在写入前通过；G1 仅适用 fixture boundary/path segregation；G2 适用
  canonical identity、strict validation和正负/冲突/unknown/tamper controls；G3 不适用（无 runtime/effect
  lifecycle）；G4 仅 `OfflineUnit`，所有 external lanes显式 `NotExecuted`；G5、G6 适用。
- Required repository checks：format/strict decode、canonical identity mutation、domain boundary、case uniqueness、
  six-partition completeness、public/restricted path audit、license/provenance、secret/path/provider-body scan、
  Markdown links和 `git diff --check`。
- Required external lanes：无。
- Documentation updates：本记录 final review、catalog/current baseline状态、DEV-002 exact input references。
- Fact-ledger update：accepted commit + public identities + redacted private review receipt identity + remaining
  not-executed scope。

### 12.1 Exact change inventory

Public Git additions, all under `fixtures/cuda-ascend/intent/reduce-sum-f32/v1/`：

| Path | Purpose |
| --- | --- |
| `README.md` | scope、clean-room method、build-not-executed 与 data classification |
| `manifest.json` | current-V1 public bundle provenance、license、obligations 和 content edges |
| `source/CMakeLists.txt` | explicit CUDA/host fixture build |
| `source/include/reduce_sum.h` | exact ABI declaration |
| `source/src/reduce_sum_kernel.cu` | Cairn-authored kernel source |
| `source/src/reduce_sum_launch.cu` | explicit host launch and bounds checks |
| `source/tests/reference_main.cpp` | fixture entry/observation writer；不是 semantic authority |
| `claims.json` | mathematical、source-tree、deployment-specialization、unknown competing hypotheses |
| `public-corpus.json` | honest、tail/order-sensitive、wrong-hypothesis、unknown controls |
| `user-decision-controls.json` | empty/domain expansion/source anomaly/caller-source conflict dispositions |
| `restricted-partitions.public.json` | 仅 partition completeness/disposition，不含 hidden identity/bytes |

Private additions, never Git-tracked, all under
`.cairn/secrets/restricted-corpora/intent/reduce-sum-f32/v1/`：

| Path | Purpose |
| --- | --- |
| `manifest.json` | exact private identities、review、freeze/exposure state |
| `implementation-artifact.json` | required sealed partition batch |
| `source-defect.json` | required sealed partition batch |
| `deployment-quirk.json` | required sealed partition batch |
| `competing-plausible-meaning.json` | required sealed partition batch |
| `genuine-unknown.json` | required sealed partition batch |
| `tamper-wrong-binding.json` | required sealed partition batch |

Existing source/tests modified or deleted：无。状态账本和本记录的 final-review字段在 acceptance commit中更新。

## 13. G0 Review Checklist

- [x] D-039/D-040/D-041 decision blockers已关闭；OQ-024 对 non-adaptive batch 不适用。
- [x] objective、non-goals、inputs、outputs、authority、effects和 exact change inventory已列出。
- [x] V1/no-compatibility、strong-type boundary、external lanes、budget与 stop policy已声明。
- [x] DEV-003 accepted commit `79a1174ad9767ab528c808a39511ada91e8129f9`及exact contract paths
  `crates/cairn-testkit/src/fixtures/provenance.rs`、`sanitation.rs`已填入本记录；DEV-001必须消费其中
  `FixtureIdentity`、`HistoricalSourceReferenceId`、`SanitationScanProfileId`和strict V1 manifest contract。
- [x] WS-DOMAIN owner确认 source/ABI/corpus contract（用户于2026-08-27接受review package `fe88f4e`）。
- [x] WS-QUALITY reviewer确认 controls、provenance、scan和删除时机（同一评审）。
- [x] WS-ADMISSION reviewer确认 sealed partition、diagnostic与 future mechanism input边界（同一评审）。
- [x] 实现提交后的worktree audit无未提交product/code改动；catalog/current baseline同步改为`Ready`。

## 14. Final Review（完成时填写）

- Accepted commit(s)：未填写
- Evidence identities/paths：未填写
- 与初始设计的偏差：未填写
- Remaining gaps：未填写
- Revalidation triggers：source/corpus bytes、D-039 contract、license/provenance、partition/exposure policy、
  canonical identity规则任一变化
- Final status：`NotStarted`
