//! Test-only support that preserves Cairn's production authority boundaries.
//!
//! The crate owns typed fixture provenance and sanitation checks. It intentionally exposes no
//! production admitted constructor, restricted-store capability, or test-only bypass into product
//! code.

pub mod fixtures;
