# ST1 identity graph fixture plan

Owner: DEV-105. Status: plan only; no current fixture artifact exists.

The future fixture must start at the frozen task and context, traverse the SIR process and
`IntentHypothesisSet`, preserve the independently derived `RequiredIntentEvidenceSet`, bind every
authorized observation and qualification receipt, cross the separate Intent Mechanical Gate, and
end at either a `MigrationIntentContract` or an explicit non-success outcome. A successful path may
then create exactly one `OracleClaimProposal` and must stop before Candidate Search.

The manifest must preserve exact artifact, event, process, policy, mechanism, environment, receipt,
and source-reference edges. Separate controls must prove that conflict and unknown outcomes cannot
enter an API requiring admitted intent, and that restart reconstructs the graph from durable facts.
The fixture is newly authored only after DEV-105 has an accepted consumer; this plan is not a
placeholder artifact and carries no admitted authority.
